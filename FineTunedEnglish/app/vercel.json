{
  "build": {
    "env": {
      "FLUTTER_VERSION": "3.22.2"
    },
    "command": "https://github.com/CristianJ23/fine-tuned-english.git --depth 1 -b stable /usr/local/flutter && export PATH=\"$PATH:/usr/local/flutter/bin\" && flutter build web --release",
    "outputDirectory": "build/web"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}